"use client";

import React from "react";
import Layout from "@/components/Layout";

const FAQ = () => {
  return (
    <Layout>
      <div className="flex-grow flex items-center justify-center p-4">
        <h1 className="text-4xl font-bold text-foreground">FAQ Page</h1>
      </div>
    </Layout>
  );
};

export default FAQ;